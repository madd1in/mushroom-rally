const http=require('http'),fs=require('fs'),path=require('path');
const root=__dirname,port=Number(process.argv[2]||4218);
const TYPES={'.js':'text/javascript','.mjs':'text/javascript','.html':'text/html','.css':'text/css','.json':'application/json','.mp3':'audio/mpeg','.wav':'audio/wav','.glb':'model/gltf-binary'};
// Range-Requests wie bei GitHub Pages, damit <audio> springen kann (BGM-Ueberblendung setzt currentTime zurueck).
http.createServer((req,res)=>{try{const p=decodeURIComponent(new URL(req.url,'http://localhost').pathname);const file=path.resolve(root,'.'+(p==='/'?'/index.html':p));if(!file.startsWith(root+path.sep)){res.writeHead(403);return res.end();}fs.readFile(file,(e,b)=>{if(e){res.writeHead(404);return res.end('Not found');}const head={'Content-Type':TYPES[path.extname(file)]||'application/octet-stream','Cache-Control':'no-cache','Accept-Ranges':'bytes'};const m=/bytes=(\d*)-(\d*)/.exec(req.headers.range||'');if(m){const start=m[1]?+m[1]:b.length-+m[2],end=m[1]&&m[2]?Math.min(+m[2],b.length-1):b.length-1;if(start>=b.length||start>end){res.writeHead(416,{'Content-Range':'bytes */'+b.length});return res.end();}res.writeHead(206,{...head,'Content-Range':`bytes ${start}-${end}/${b.length}`,'Content-Length':end-start+1});return res.end(b.subarray(start,end+1));}res.writeHead(200,{...head,'Content-Length':b.length});res.end(b);});}catch{res.writeHead(400);res.end();}}).listen(port,'127.0.0.1',()=>console.log('Mushroom Rally: http://127.0.0.1:'+port));
